# ============================================
# CMPC LIMS - Production Environment Variables
# ============================================
# Copia este archivo a .env.production y completa los valores

# Database Configuration
POSTGRES_DB=cmpc_lims
POSTGRES_USER=cmpc_user
POSTGRES_PASSWORD=CAMBIAR_PASSWORD_SEGURO_AQUI

# Backend Configuration
BACKEND_PORT=3000
JWT_SECRET=CAMBIAR_SECRET_SEGURO_AQUI_MIN_32_CARACTERES
JWT_REFRESH_SECRET=CAMBIAR_REFRESH_SECRET_AQUI_MIN_32_CARACTERES
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# SMTP Configuration (para notificaciones por email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-email@gmail.com
SMTP_PASSWORD=tu-app-password-de-gmail
SMTP_FROM_EMAIL=noreply@cmpc.cl
SMTP_FROM_NAME=CMPC LIMS

# Frontend Configuration
FRONTEND_PORT=80
FRONTEND_URL=http://tu-servidor.com
VITE_API_URL=http://tu-servidor.com:3000/api

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. JWT_SECRET y JWT_REFRESH_SECRET deben ser strings aleatorios largos y seguros
#    Puedes generarlos con: openssl rand -base64 32
#
# 2. Para Gmail SMTP, necesitas crear una "App Password":
#    https://support.google.com/accounts/answer/185833
#
# 3. FRONTEND_URL debe ser la URL pública de tu servidor (sin puerto si usa 80/443)
#
# 4. VITE_API_URL debe apuntar a tu backend API
#
# 5. NUNCA subas el archivo .env.production a Git
